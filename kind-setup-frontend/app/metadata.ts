import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Home',
  description: 'Kind Setup - A modern interface for managing Kind Kubernetes clusters',
  keywords: ['home', 'kubernetes', 'kind', 'cluster', 'management', 'dashboard'],
};
