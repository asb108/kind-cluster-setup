import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Settings | Kind Cluster Setup',
  description: 'Configure application settings, user preferences, and system configuration for Kind Cluster Setup',
  keywords: ['settings', 'configuration', 'preferences', 'kind', 'cluster management', 'kubernetes'],
}
