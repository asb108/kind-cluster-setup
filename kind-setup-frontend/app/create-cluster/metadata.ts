import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Create Cluster',
  description: 'Create a new Kind Kubernetes cluster with custom configuration',
  keywords: ['create', 'kubernetes', 'kind', 'cluster', 'configuration'],
};
