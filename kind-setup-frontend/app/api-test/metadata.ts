import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'API Test',
  description: 'Test the Kind Setup API endpoints',
  keywords: ['api', 'test', 'kubernetes', 'kind', 'cluster', 'endpoints'],
};
