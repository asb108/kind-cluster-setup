import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Airflow',
  description:
    'Deploy and manage Apache Airflow on your Kind Kubernetes clusters',
  keywords: ['airflow', 'apache', 'kubernetes', 'kind', 'cluster', 'workflow'],
};
