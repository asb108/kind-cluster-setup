import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Deploy Application',
  description: 'Deploy applications to your Kind Kubernetes clusters',
  keywords: ['deploy', 'application', 'kubernetes', 'kind', 'cluster'],
};
